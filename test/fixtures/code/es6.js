const a = 1212;
const b = 43535;
const util = require('util');
/**
 * @param man <human>
 * @param {Boolean} flag
 * @returns {number}
 */
const convertShopToClient = (man, flag) => {
  man.age = 34;
}