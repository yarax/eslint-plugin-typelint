const a = 1212;
const b = 43535;
const util = require('util');
const convertShopToClient = (man, flag) => {
  /**
   * OLOLO
   * @param man [shop]
   * @param {Boolean} flag
   * @returns {number}
   */
  man.data.fuck = 34;
}