const a = 1212;
const b = 43535;
const util = require('util');
const convertShopToClient = (data, flag) => {
  /**
   * OLOLO
   * @param data [campaign_group.data]
   * @param {Boolean} flag
   * @returns {number}
   */
  var a = data.fuck;
}