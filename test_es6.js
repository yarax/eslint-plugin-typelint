const a = 1212;
const b = 43535;
const util = require('util');
/**
 * OLOLO
 * @param man [shop]
 * @param {Boolean} flag
 * @returns {number}
 */
const convertShopToClient = (man, flag) => {
  man.data.integration_type = 34;
}